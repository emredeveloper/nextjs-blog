// Bu dosya, kullanıcı doğrulama işlemleri için kullanılabilir
export function authenticateUser(email, password) {
    // Bu işlemi veritabanı ile yapmalısınız
    return email === 'user@example.com' && password === 'password';
  }
  